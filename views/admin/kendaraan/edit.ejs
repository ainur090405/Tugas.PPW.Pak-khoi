<!DOCTYPE html>
<html>
<head>
  <title>Edit Kendaraan</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
  <div class="card shadow border-0">
    <div class="card-header bg-primary text-white">
      <h4 class="mb-0">Edit Kendaraan</h4>
    </div>
    <div class="card-body">
      <form action="/admin/kendaraan/edit/<%= kendaraan.id_kendaraan %>" method="POST">
        
        <!-- Plat Nomor -->
        <div class="mb-3">
          <label class="form-label">Plat Nomor</label>
          <input type="text" name="opol" class="form-control" value="<%= kendaraan.opol %>" required>
        </div>

        <!-- Jenis Kendaraan -->
        <div class="mb-3">
          <label class="form-label">Jenis Kendaraan</label>
          <select name="type" class="form-control" required>
            <option value="mobil" <%= kendaraan.type === 'mobil' ? 'selected' : '' %>>Mobil</option>
            <option value="motor" <%= kendaraan.type === 'motor' ? 'selected' : '' %>>Motor</option>
          </select>
        </div>

        <!-- Warna -->
        <div class="mb-3">
          <label class="form-label">Warna</label>
          <input type="text" name="warna" class="form-control" value="<%= kendaraan.warna %>" required>
        </div>

        <!-- Tarif -->
        <div class="mb-3">
          <label class="form-label">Tarif</label>
          <select name="id_tarif" class="form-control" required>
            <% tarif.forEach(t => { %>
              <option value="<%= t.id_tarif %>" <%= kendaraan.id_tarif === t.id_tarif ? 'selected' : '' %>>
                <%= t.type %> - Rp <%= t.harga %> / <%= t.satuan_waktu %>
              </option>
            <% }) %>
          </select>
        </div>

        <!-- Status -->
        <div class="mb-3">
          <label class="form-label">Status</label>
          <select name="status" class="form-control" required>
            <option value="in" <%= kendaraan.status === 'in' ? 'selected' : '' %>>IN</option>
            <option value="out" <%= kendaraan.status === 'out' ? 'selected' : '' %>>OUT</option>
          </select>
        </div>

        <!-- Tombol -->
        <div class="d-flex justify-content-between">
          <a href="/admin/kendaraan" class="btn btn-secondary">⬅ Kembali</a>
          <button type="submit" class="btn btn-success">💾 Simpan Perubahan</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
